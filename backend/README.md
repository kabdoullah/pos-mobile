# POS Mobile Backend

FastAPI backend for POS Mobile — point of sale system for Ivorian PMEs.

## Quick start

```bash
make dev           # Start server with hot reload
make migrate       # Apply database migrations
make test          # Run tests (requires Docker)
```

See `CLAUDE.md` for detailed development guidelines.
